<Window x:Class="UGTLive.SetupLogViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Setup Log Viewer" 
        Height="500" 
        Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="#1A1A2E">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   x:Name="titleText"
                   Text="📋 Detailed Setup Log"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Foreground="#E94560"
                   FontFamily="Segoe UI"
                   Margin="0,0,0,8"/>
        
        <!-- Info Banner -->
        <Border Grid.Row="1"
                Background="#16213E"
                BorderBrush="#0F3460"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Margin="0,0,0,10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                    <TextBlock Text="💡"
                               FontSize="14"
                               Margin="0,0,8,0"
                               VerticalAlignment="Center"/>
                    <TextBlock x:Name="infoText"
                               Text="This log auto-updates as the installation progresses. Useful for diagnosing stuck installations."
                               FontSize="12"
                               Foreground="#94A3B8"
                               TextWrapping="Wrap"
                               VerticalAlignment="Center"
                               FontFamily="Segoe UI"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📁 File:"
                               FontSize="11"
                               Foreground="#64748B"
                               Margin="0,0,6,0"
                               VerticalAlignment="Center"
                               FontFamily="Segoe UI"/>
                    <TextBox x:Name="filePathTextBox"
                             Text=""
                             FontSize="11"
                             Foreground="#7DD3FC"
                             Background="Transparent"
                             BorderThickness="0"
                             IsReadOnly="True"
                             FontFamily="Cascadia Code, Consolas"
                             VerticalAlignment="Center"
                             ToolTip="You can copy this path to open the file manually"/>
                </StackPanel>
            </StackPanel>
        </Border>
        
        <!-- Log Content -->
        <Border Grid.Row="2"
                Background="#0F0F1A"
                BorderBrush="#2D2D44"
                BorderThickness="1"
                CornerRadius="4"
                Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          x:Name="logScrollViewer">
                <TextBox x:Name="logTextBox"
                         Background="Transparent"
                         Foreground="#A5D6A7"
                         FontFamily="Cascadia Code, Consolas, Courier New"
                         FontSize="11"
                         BorderThickness="0"
                         IsReadOnly="True"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Disabled"
                         HorizontalScrollBarVisibility="Disabled"
                         Padding="10"/>
            </ScrollViewer>
        </Border>
        
        <!-- Bottom Bar -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Status indicator -->
            <StackPanel Grid.Column="0" 
                        Orientation="Horizontal" 
                        VerticalAlignment="Center">
                <Ellipse x:Name="statusIndicator"
                         Width="8" Height="8"
                         Fill="#4CAF50"
                         Margin="0,0,8,0"/>
                <TextBlock x:Name="statusText"
                           Text="Watching for changes..."
                           FontSize="11"
                           Foreground="#64748B"
                           VerticalAlignment="Center"
                           FontFamily="Segoe UI"/>
            </StackPanel>
            
            <!-- Close Button -->
            <Button Grid.Column="1"
                    x:Name="closeButton"
                    Content="Close"
                    Width="90"
                    Height="28"
                    Click="CloseButton_Click"
                    Background="#E94560"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    FontFamily="Segoe UI"
                    FontWeight="SemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </Grid>
    </Grid>
</Window>

